<li class="list-group-item">
	{{id}}.
	{{burger_name}}

	<p class="devoured" data-id="{{id}}" data-burger_name="{{burger_name}}" data-newDevoured="{{true}}">
		{{#unless devoured}}EAT ME!{{/unless}}
	</p>
</li>
